import { DashboardLayout } from "@/components/dashboard-layout";

export default function TermsPage() {
  return (
    <DashboardLayout breadcrumb={{ items: [{ label: "利用規約" }] }}>
      <div />
    </DashboardLayout>
  );
}
