import { DashboardLayout } from "@/components/dashboard-layout";

export default function ScriptsPage() {
  return (
    <DashboardLayout breadcrumb={{ items: [{ label: "スクリプト" }] }}>
      <div />
    </DashboardLayout>
  );
}
